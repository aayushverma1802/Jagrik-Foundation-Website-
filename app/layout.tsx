import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Jagrik Foundation - Supporting Mental Health & Community",
  description: "Join us in making a difference. Support mental health initiatives and community programs through your generous donations.",
  keywords: "donation, mental health, charity, support, community, foundation",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

